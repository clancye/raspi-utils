This script clones the repo from git into the current directory
and runs the install commands found on the website:

cpputest.github.io

You'll need automake, autoconf, and libtool:
apt-get install automake autoconf libtool
